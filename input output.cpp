#include <iostream>
using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;          // Read three integers
    int sum = a + b + c;         // Calculate the sum
    cout << sum << endl;         // Output the result
    return 0;
}
